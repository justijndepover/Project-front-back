{
  "name": "spacewars",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "prestart": "start /b mongod --dbpath data",
    "start": "node ./bin/www",
    "poststop": "mongo admin --eval 'db.shutdownServer()' > /dev/null",
    "pretest": "start /b mongod --dbpath data",
    "test": "mocha test",
    "posttest": "mongo admin --eval 'db.shutdownServer()' > /dev/null"
  },
  "dependencies": {
    "body-parser": "~1.13.2",
    "cookie-parser": "~1.3.5",
    "debug": "~2.2.0",
    "express": "~4.10.2",
    "jade": "~1.11.0",
    "mongoose": "^4.3.4",
    "morgan": "~1.6.1",
    "serve-favicon": "~2.3.0"
  }
}
